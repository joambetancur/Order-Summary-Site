# 🎶 Order Summary Component

Este es un componente de tarjeta de resumen de pedido simple, construido con HTML y CSS. Fue desarrollado como solución a un desafío de maquetación de Frontend Mentor.

## 🚀 Tecnologías Utilizadas

* **HTML5**
* **CSS3** (Incluyendo Variables CSS y Flexbox)
* **Fuente:** Red Hat Display, importada desde Google Fonts.

## ✨ Características del Componente

El diseño se enfoca en la limpieza y la usabilidad, siguiendo la guía de estilo del desafío:

* **Paleta de Colores:** Se han definido variables CSS para gestionar los colores de manera eficiente:
    * `--pale-blue`: `hsl(225, 100%, 94%)`
    * `--bright-blue`: `hsl(245, 75%, 52%)`
    * `--very-pale-blue`: `hsl(225, 100%, 98%)`
    * `--desaturated-blue`: `hsl(224, 23%, 55%)`
    * `--dark-blue`: `hsl(223, 47%, 23%)`
* **Efectos Hover:** Se incluyen efectos de interacción para los botones (`.proceed-button`, `.cancel-button`) y el enlace "Change".
* **Estructura Semántica:** Uso de contenedores claros para la imagen hero (`.hero-img`), el contenido (`.container`), y el detalle del plan (`.plan-container`).
